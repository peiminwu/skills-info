version: 1
interface:
  display_name: XHS Content Extractor For Mac
  short_description: macOS 专用，抓取小红书图文输出 TXT，并在生成后继续追问是否发送到kindle
  default_prompt: 输入一个或多个小红书图文链接，抓取正文并将每张图片通过 Apple Vision OCR 后按原顺序输出为 txt；生成后继续询问是否逐个发送到kindle，或合并成合集后发送到kindle。
