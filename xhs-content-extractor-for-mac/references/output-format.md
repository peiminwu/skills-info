# Output format

TXT 文件编码：`UTF-8 with BOM`（`utf-8-sig`）。
默认文件名：`《笔记标题》.txt`。

结构：
1. 标题
2. 作者（若存在）
3. 正文段落（按顺序）
4. 图片块（按顺序）

图片块模板：直接插入 Apple Vision OCR 文本，不添加前后标记。

OCR 失败模板：`[图片N OCR失败：<错误原因>]`

OCR 文本后处理：
1. 自动合并 OCR 造成的碎换行（段内连行）。
2. 识别到空行时保留段落分隔。
